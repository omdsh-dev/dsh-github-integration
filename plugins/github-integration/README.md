# GitHub Integration Skill

The installable source is `.dsh-plugin/skills/github-issue-pr/SKILL.md`. It uses the public Workshop Skill manifest and has no package lifecycle scripts or generated Repository Plugin wrapper.

Run `node scripts/verify-release.mjs` from the repository root to perform the static admission checks. A passing result proves the Skill contract and source safety only; it does not grant Registry installation authority or GitHub permissions.
